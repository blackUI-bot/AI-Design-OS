# 字体工作流 V1.0

> 适用范围：任何基于参考图持续生成新文案的中文字体、标题字、手写字、艺术字、字标或风格化文字工作流。
>
> 核心目标：让不同时间、不同文案、不同批次生成的文字保持为同一套字体家族；相同整字、偏旁、部件和关键笔画不得在不同文案中无理由变形。

---

## 1. 工作流的基本定义

本工作流不是“每次参考一张图重新设计一组相似文字”，而是围绕一张主参考图，持续建立并扩充一套可复用的字形系统。

每次生成都必须同时完成两件事：

1. **保持风格一致**：字形骨架、字重、比例、圆角、重心、节奏、装饰尺度和排版气质与主参考图一致。
2. **保持资产一致**：已经确认过的整字、偏旁、部件及特征笔画必须优先直接沿用，不能每次重新设计。

最终形成的不是若干张“风格接近”的独立图片，而是一套逐步扩充、可以持续复用的字体资产库。

---

## 2. 工作流创建流程

### 2.1 确立主参考图

用户第一次提供字体图片并明确“以这个字体风格为标准”时，将该图登记为唯一的**主参考图（Master Reference）**。

主参考图决定整套工作流的最高级视觉规则，包括：

- 字体类别与整体气质
- 字形基础骨架
- 字面宽窄、高矮和比例
- 字重与笔画粗细关系
- 横、竖、撇、捺、点、钩、折等基础笔画形态
- 转角、端点和连接方式
- 内部字腔的形状与大小
- 字形重心、倾斜度及稳定性
- 字与字之间的视觉节奏
- 手工感、几何感、书写感或印刷感的程度
- 装饰性变形的数量、位置和强度
- 默认颜色、背景、排版方向、留白及输出比例

主参考图一经确立，后续所有补充图都只能扩展它，不能重新定义它。

### 2.2 为工作流命名和建档

建议采用以下命名方式：

`Master｜字体风格名称 V1.0`

建档时至少记录：

| 项目 | 记录内容 |
| --- | --- |
| 工作流名称 | 字体风格名称与版本号 |
| 主参考图 | 文件、图片或对话中的明确位置 |
| 核心风格 | 用简洁、可验证的语言描述 |
| 默认输出 | 字色、背景、横竖排、是否折行、画布比例、留白 |
| 禁止项 | 不属于该风格的效果或常见错误 |
| 当前资产 | 已确认的整字、偏旁、部件和特征笔画 |

### 2.3 首次风格拆解

创建工作流时，应先从主参考图提取稳定规则，不要只写“年轻、好看、有设计感”等主观词。

建议从以下维度拆解：

#### A. 骨架

- 字形偏方、偏圆、偏宽、偏窄或偏斜
- 中宫是紧还是松
- 横向与纵向的伸展关系
- 字形是否压扁、拉高或存在大小错落

#### B. 笔画

- 主笔画的粗细与粗细变化
- 端点是平切、圆头、尖头还是不规则手工形
- 转折是硬折、圆折、切角还是连笔
- 点、钩、撇、捺等是否有固定造型

#### C. 结构

- 左右结构、上下结构、包围结构的比例
- 偏旁是否缩窄、抬高、下沉或变形
- 部件之间的距离和穿插方式
- 重心位于几何中心还是有意偏移

#### D. 设计语言

- 几何化、手写化、拟物化或符号化程度
- 异形字腔、断笔、圆点、缺口、延长笔画等设计点的使用逻辑
- 一句话中允许出现多少个明显设计点
- 设计点是结构性的还是纯装饰性的

#### E. 排版与输出

- 单行或多行
- 字距、行距、对齐方式
- 字号之间是否有层级
- 背景与字体颜色
- 是否允许英文、副标题、图形、阴影、纹理或立体效果

风格拆解的原则是：每条规则都应能在生成结果中被检查，而不是只用于形容氛围。

---

## 3. 参考图体系与优先级

### 3.1 参考图分级

工作流中的参考图分为三级：

| 等级 | 类型 | 作用 | 权限 |
| --- | --- | --- | --- |
| P0 | 主参考图 | 定义整体风格、骨架、字重和设计尺度 | 最高，不被补充图覆盖 |
| P1 | 已确认补充参考图 | 补充新的整字、偏旁、部件和排版案例 | 可扩充资产，不可改变主风格 |
| P2 | 临时参考图 | 只参考用户明确指定的局部信息 | 默认不并入长期资产库 |

### 3.2 主参考图的最高优先级

如果补充参考图与主参考图出现差异，必须按以下顺序处理：

1. 保留主参考图的骨架、字重、比例、转角和整体气质。
2. 仅吸收补充图中已被用户认可的整字或局部部件。
3. 对与主图不一致的局部进行主风格校正后再登记。
4. 不得因为补充图数量更多，就用补充图的平均风格替代主参考图。

可概括为：

> 主参考图定义“这套字体是什么”，补充参考图只回答“这套字体还能怎样写更多字”。

### 3.3 补充参考图的准入规则

用户主动提供新图片时，不应默认把所有图片都视为同等参考。只有满足以下条件之一，才可登记为补充参考图：

- 用户明确说“作为补充参考图”“加入工作流”或同等意思；
- 用户明确认可某次生成结果，并要求后续沿用；
- 用户指定图中的某个整字、偏旁或笔画作为固定标准。

补充图加入前要检查：

- 是否仍属于主参考图的风格体系；
- 是否存在过度装饰、错误字形或风格漂移；
- 哪些整字和部件可以继承；
- 哪些内容只适用于当前排版，不应固化；
- 是否与已有资产发生冲突。

### 3.4 补充图与主图有差异时

当补充图整体可用但局部偏离主图时，采用“局部收录”而不是“整图覆盖”：

- 可收录：用户明确认可的整字、偏旁、部件、笔画或排版关系；
- 不收录：与主图不同的字重、圆角尺度、倾斜程度、装饰密度或整体气质；
- 必要时先把可用部件校正到主图规则，再作为标准资产保存。

如果无法判断差异是有意扩展还是风格偏离，应先询问用户，不得自行提高补充图的优先级。

### 3.5 用户持续补图时的版本管理

每次新增被确认的补充图后，更新工作流版本：

- 新增参考图或少量资产：`V1.0 → V1.1`
- 修改重要部件标准或生成规则：`V1.1 → V1.2`
- 更换主参考图或重构整体风格：`V1.x → V2.0`

每次更新应记录：

- 新增了哪些参考图；
- 新增或替换了哪些整字、偏旁和部件；
- 哪些旧资产仍然有效；
- 是否新增例外规则；
- 用户明确认可了哪一版结果。

---

## 4. 字形资产库

### 4.1 资产的四个层级

字体资产按以下层级记录：

1. **整字资产**：完整字形，如“深”“小”“途”。
2. **偏旁与部件资产**：如三点水、提手旁、走之底、酉部、心部。
3. **特征笔画资产**：如固定圆点、横折、弧形收笔、长撇、特殊竖钩。
4. **结构模板资产**：如左右结构的宽度比例、上下结构的重心和包围结构的开口方式。

### 4.2 资产登记表

建议维护如下表格：

| 资产 ID | 类型 | 内容 | 来源 | 状态 | 固定特征 | 可变范围 |
| --- | --- | --- | --- | --- | --- | --- |
| C-001 | 整字 | 小 | 主参考图 | 锁定 | 三笔造型、比例、位置 | 仅允许等比缩放 |
| R-001 | 偏旁 | 氵 | 主参考图中的“深” | 锁定 | 三笔形状、大小关系、方向与间距 | 可随结构整体缩放 |
| R-002 | 部件 | 辶 | 补充图中的“途” | 锁定 | 圆点、低位转折、横向弧形收笔 | 可按字宽调整末端长度 |
| S-001 | 结构 | 左右结构 | 多个确认字形 | 有效 | 左窄右宽、视觉居中 | 依据部件复杂度微调 |

资产状态建议分为：

- **锁定**：来自主图或被用户明确认可，后续必须复用；
- **有效**：当前可使用，但仍可在用户反馈后微调；
- **候选**：新生成但尚未被用户确认，不进入固定复用范围；
- **废弃**：用户否定或已被新标准替换，后续禁止使用。

### 4.3 用户认可即固化

当用户对修正后的字形明确表示“这样是对的”“以后按这个来”或同等意思时，应立即：

1. 将对应整字或部件从“候选”改为“锁定”；
2. 记录它的来源图和被认可的版本；
3. 覆盖或废弃此前错误版本；
4. 后续命中相同资产时直接调用，不再重新推导。

---

## 5. 整字、偏旁与部件级复用规则

### 5.1 总原则

复用优先级从高到低为：

1. 已锁定的相同整字；
2. 已锁定的相同偏旁或部件；
3. 已锁定的特征笔画；
4. 已确认的结构模板；
5. 依据主参考图为新字推导字形。

只要上一级资产存在，就不得跳过它重新设计。

### 5.2 相同整字的复用

不同文案中出现同一个字时：

- 优先直接沿用已确认的完整字形；
- 保持骨架、笔画形态、内部空间、设计点和重心一致；
- 只允许为排版进行等比缩放或非常有限的字距调整；
- 不得因为上下文不同而重新生成另一种写法；
- 如果确实需要异体版本，必须由用户明确批准，并单独命名为变体。

例如，参考图中已经存在“光”，新文案再次出现“光”时，应复用原有“光”，而不是重新生成一个“风格相似的光”。

### 5.3 相同偏旁或部件的复用

不同汉字包含相同偏旁时，应进行真正的**部件级复用**，而不是只参考抽象风格。

必须尽量保持：

- 部件的具体轮廓；
- 笔画数量和形状；
- 点、折、钩、收笔的具体方式；
- 部件内部各笔的大小关系；
- 笔画方向、角度和间距；
- 部件在字框中的相对位置；
- 与另一半结构连接或避让的方式。

允许变化的范围通常只有：

- 为适配新字结构进行整体等比缩放；
- 在不改变识别特征的前提下，略微压缩或拉伸部件宽度；
- 根据另一侧部件复杂度，微调部件间距；
- 延长不影响特征的末端笔画以维持整字平衡。

不允许的做法：

- 把已有异形部件替换成常规字体写法；
- 只保留“圆角、粗体”等表面风格，却改变部件的具体结构；
- 同一个偏旁在不同文案中出现多种未经批准的造型；
- 为追求新鲜感而故意重画已确认部件。

### 5.4 部件级复用示例

以“途”中的走之底用于“追”为例，正确做法是：

- 直接继承“途”的走之底部件；
- 保留其圆点造型；
- 保留低位转折的位置；
- 保留横向弧形收笔的方向和形态；
- 仅根据“追”上半部的宽度调整末端长度和整体占位。

错误做法是：仅生成一个普通圆角走之底，然后声称它“参考了途的风格”。

同理，“深”中的三点水用于“酒、沿、潮”等字时，应复用三笔的具体形状、大小关系、方向和位置节奏，而不只是统一成“三个圆角点”。

### 5.5 同一句中的重复部件

同一句文案中如果多个字拥有相同部件，例如“探”和“指”都含提手旁：

- 两个提手旁必须来自同一个标准资产；
- 不应分别生成后再人工判断是否相似；
- 优先先确定部件，再组合各自右侧结构；
- 完成后对两个部件进行叠加或并排比对。

### 5.6 同一部件存在多个历史版本时

如果历史图片中同一部件已经出现多个版本，按以下顺序选择：

1. 用户最后明确认可并锁定的版本；
2. 主参考图中的版本；
3. 优先级最高的补充参考图版本；
4. 与主参考图整体规则最接近的版本。

不得随机混用。需要保留多个版本时，必须标记适用条件，例如“标准版”“窄结构版”“上下结构版”，且各版本仍需共享相同的识别特征。

---

## 6. 新字与新部件的推导规则

### 6.1 先复用，再创造

面对新文案时，先把每个字拆分并查询资产库：

- 哪些字已有完整字形；
- 哪些字虽然没有整字，但包含已有偏旁或部件；
- 哪些笔画可调用现有特征笔画；
- 哪些结构完全没有先例，需要根据主参考图推导。

只有不存在可复用资产的部分，才允许新设计。

### 6.2 新字推导顺序

新字应按以下顺序构建：

1. 确定字的标准结构和正确笔画，保证可识别性；
2. 套用主参考图的骨架比例和字重；
3. 植入已有偏旁、部件和特征笔画；
4. 按已有结构模板调整中宫、重心和留白；
5. 判断是否真的需要设计点；
6. 用主参考图的装饰尺度进行克制处理；
7. 与同批及历史字形进行一致性比对。

### 6.3 设计点的控制

字体风格不等于每个字都必须有异形设计。

默认规则：

- 先保证基础骨架和整体气质；
- 设计点应服务于结构、语义或视觉节奏；
- 没有充分理由时，保持字形干净；
- 设计点的密度不得高于主参考图；
- 同一句中只在少数关键字上做明显变化；
- 不得通过堆叠圆点、缺口、图标、延长线等方式制造“像设计过”的假象。

如果主参考图本身装饰克制，则新字也必须克制。补充图中偶然出现的高装饰字形，不能反向提高整套字体的装饰密度。

### 6.4 可识别性优先

任何风格化处理都不得破坏：

- 汉字基本结构；
- 正确笔画关系；
- 部件边界；
- 常规阅读顺序；
- 字与相似字之间的区分。

生僻字或复杂字应优先保证正确和清晰，再逐步加入风格特征。

---

## 7. 每次生成的标准执行流程

### 第一步：读取当前标准

生成前必须确认：

- 当前工作流名称和版本；
- 主参考图是哪一张；
- 当前有效的补充参考图；
- 用户最近一次确认或纠正的规则；
- 默认输出格式是否被本次指令覆盖。

### 第二步：解析文案

检查：

- 文案是否准确，有无错别字；
- 字符数量；
- 是否允许折行；
- 是否包含英文、数字、标点或特殊符号；
- 是否存在生僻字或图像模型容易误写的复杂字。

### 第三步：建立复用清单

生成前列出内部执行清单：

| 字符 | 整字复用 | 部件复用 | 新设计内容 | 来源 |
| --- | --- | --- | --- | --- |
| 示例字 A | 是/否 | 部件名称 | 尚缺部分 | 主图/补充图/确认稿 |

凡是已存在的整字或部件，都要明确来源。不得在没有查询资产的情况下直接整句生成。

### 第四步：确定排版

按照用户本次要求和工作流默认值确定：

- 单行或多行；
- 画布比例；
- 字号与总宽度；
- 字距和行距；
- 对齐与视觉中心；
- 字色、背景色和留白；
- 是否需要透明背景。

排版变化不能改变字形资产本身。

### 第五步：生成或组装

生成时按“锁定资产优先”的方式构建：

1. 放置可复用的完整字形；
2. 组装可复用偏旁和部件；
3. 仅生成缺失的新结构；
4. 用主参考图统一字重、圆角、比例和节奏；
5. 最后处理排版，不在排版阶段随意重画字形。

如果所用工具无法真正锁定或复制局部部件，应在生成指令中明确指定部件来源、具体结构和禁止变化项，并在生成后重点检查。不能把模型的“看起来接近”视为已完成复用。

### 第六步：执行自检查

输出给用户前，必须完成第 8 节的完整检查。发现不合格时应先定向修正，不应把明显偏离的初稿直接作为完成稿。

### 第七步：接收反馈并更新资产

- 用户认可：把新增整字或部件登记为锁定资产；
- 用户要求微调：只修改指定范围，其他已确认部分保持不变；
- 用户指出未复用：回到具体来源部件做定向替换，不能重新生成整句掩盖问题；
- 用户否定：将错误版本标为废弃，防止以后再次引用。

---

## 8. 生成后自检查清单

### 8.1 文字正确性

- [ ] 文案字数、顺序、标点完全正确
- [ ] 没有错字、漏字、多字或形近字替换
- [ ] 每个字的基本结构与笔画关系正确
- [ ] 复杂字和生僻字清晰可识别

### 8.2 主风格一致性

- [ ] 骨架宽窄、高矮与主参考图一致
- [ ] 字重及横竖笔画关系一致
- [ ] 端点、转角、圆角或切角语言一致
- [ ] 中宫、字腔和重心符合主图规律
- [ ] 手工感、几何感、倾斜度等没有漂移
- [ ] 装饰密度没有高于主参考图
- [ ] 整体看起来属于同一个字体家族，而非相似风格拼接

### 8.3 资产复用一致性

- [ ] 已出现的相同整字直接沿用了锁定版本
- [ ] 已出现的相同偏旁使用了同一部件资产
- [ ] 部件的轮廓、转折、端点和收笔与来源一致
- [ ] 同一句中的重复部件彼此一致
- [ ] 没有把特征部件替换为常规字体写法
- [ ] 仅在允许范围内进行了缩放、压缩或间距适配
- [ ] 历史上被废弃的版本没有再次出现

### 8.4 新字合理性

- [ ] 新字先继承已有部件，再设计缺失部分
- [ ] 新设计部分遵循主参考图的骨架与笔画规则
- [ ] 新设计点有明确作用，不是无意义装饰
- [ ] 新字与历史资产并排时没有突兀感
- [ ] 没有为了“每个字都独特”而破坏统一性

### 8.5 排版与输出

- [ ] 单行或折行符合用户要求
- [ ] 字距、行距和视觉重心平衡
- [ ] 画布比例、背景、字色和透明度正确
- [ ] 没有擅自添加英文、图形、阴影、纹理或立体效果
- [ ] 图片边缘和留白充足，没有裁切笔画
- [ ] 输出清晰度满足使用场景

### 8.6 跨文案一致性终检

将本次结果与至少以下内容并排检查：

1. 主参考图；
2. 含有相同整字的历史确认稿；
3. 含有相同偏旁或部件的历史确认稿；
4. 最近一张被用户认可的补充参考图。

重点回答三个问题：

- 如果遮住文案内容，整体是否仍像同一套字体？
- 如果把相同部件叠加，核心轮廓是否一致？
- 新增字形是否在扩充字体，而不是另起一种风格？

任意一项答案为“否”，都应先修正再输出。

---

## 9. 定向修正规则

当用户指出某个字或部件不正确时，采用最小范围修改：

1. 明确错误对象，例如“追的走之底未沿用途的部件”；
2. 找到正确资产来源；
3. 锁定其他已确认的字、部件、排版和画面；
4. 只替换错误部分；
5. 对比替换前后，确认未引发连带变化；
6. 用户确认后，将修正版登记为新标准。

禁止做法：

- 因一个部件错误而重新随机生成整张图；
- 修正一个字时改变其他已确认字形；
- 口头声称“已参考”，但具体轮廓仍未复用；
- 用新的近似版本覆盖用户已认可的标准版本。

---

## 10. 冲突与例外处理

### 10.1 用户本次指令与默认规则冲突

用户本次明确要求的排版、颜色、背景、折行等，可以临时覆盖默认输出规则，但不会自动改变字体骨架和长期资产。

### 10.2 用户要求新的字形变体

如果用户明确希望同一个字有另一种写法：

- 保留原标准版本；
- 新版本标记为变体，例如 `光-A`、`光-B`；
- 记录变体适用条件；
- 未指定时仍默认使用标准版本。

### 10.3 新补充图与锁定资产冲突

不得自动覆盖。应优先保留锁定资产，并询问用户：

- 保留原版；
- 用新图替换为新的唯一标准；
- 两版并存并设置不同适用条件。

### 10.4 工具能力限制

如果图像生成工具无法保证整字或部件像素级不变，应如实按“高一致性复现”执行，并通过：

- 更明确的局部来源描述；
- 局部编辑而非整图重生；
- 多轮定向修正；
- 将已确认字形作为直接视觉参考；
- 必要时转入可编辑矢量或人工拼装流程；

尽可能降低漂移。不能因为工具存在随机性，就取消资产复用标准。

---

## 11. 建议的工作流记录模板

```md
# Master｜[字体风格名称] V[版本号]

## 主参考图（P0）
- 名称：
- 来源：
- 确认日期：
- 作用：定义整体骨架、字重、比例、笔画和设计尺度。

## 补充参考图（P1）
1. [图片名称]
   - 新增整字：
   - 新增偏旁/部件：
   - 仅参考内容：
   - 不继承内容：

## 核心风格规则
- 骨架：
- 字重：
- 端点与转角：
- 中宫与重心：
- 设计点尺度：
- 排版节奏：

## 默认输出规则
- 字色：
- 背景：
- 排版：
- 画布比例：
- 留白：
- 禁止项：

## 锁定整字资产
| 字 | 来源 | 固定特征 | 允许变化 |
| --- | --- | --- | --- |

## 锁定偏旁与部件资产
| 部件 | 来源字 | 固定特征 | 允许变化 |
| --- | --- | --- | --- |

## 候选资产
| 内容 | 来源 | 待确认事项 |
| --- | --- | --- |

## 废弃资产
| 内容 | 原因 | 替代标准 |
| --- | --- | --- |

## 用户确认记录
- [日期/轮次]：

## 版本更新记录
- V1.0：
```

---

## 12. 单次生成任务模板

每次开始生成前，可使用以下内部任务格式：

```md
文案：[本次文案]
工作流：[名称与版本]
主参考图：[P0]
补充参考图：[P1 列表]

整字复用：
- [字] ← [来源]

部件复用：
- [新字中的部件] ← [来源字中的锁定部件]

新设计：
- [字/部分]：按主参考图的[骨架/笔画/结构模板]推导

本次排版：
- [单行/折行、颜色、背景、比例、留白]

禁止变化：
- 已锁定整字和部件不得重画
- 不增加主参考图没有的装饰
- 不改变已确认的输出规范

生成后重点检查：
- [列出本次最容易漂移的字或部件]
```

---

## 13. 工作流的最终判断标准

一套合格的字体生成工作流，应同时满足：

- **同源性**：所有文案都能明显看出来自同一主参考图；
- **复用性**：相同整字和部件有稳定、可追溯的标准版本；
- **扩展性**：新参考图可以增加资产，但不会稀释主风格；
- **可修正性**：局部错误能够定向修改，不破坏其他已确认内容；
- **可追溯性**：每个关键字形和部件都知道来源、状态与版本；
- **克制性**：新设计服从整体系统，不因追求变化而过度装饰；
- **一致性**：跨文案、跨批次、跨时间生成时，同一部件不会出现多个未经批准的样子。

最终原则：

> 主参考图控制风格上限，补充参考图负责扩充资产；已有资产优先复用，新内容按主图推导；先统一，再创新；先检查具体部件，再判断整体感觉。
